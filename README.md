# DO NOT EDIT MANUALLY - generated by DOGMA prompt rector - 2025-11-17T00:00:00Z

# DOGMA System (esqueleto)

Proyecto de ejemplo generado automáticamente por el prompt rector. Contiene microservicios, templates e2b, código compartido y documentación mínima.

Ver `INSTALLATION.md` para instrucciones de instalación y verificación.

# DOCTRINA-STAKAS (Full Skeleton)

Stack: Python (FastAPI) + Postgres + MinIO (S3-compatible) + Docker Compose + GitHub Actions.

IMPORTANT: **Do not commit real secrets.** Use `.env.example` as reference and store secrets in GitHub Secrets or your platform secret manager.

Quickstart:
1. Create a GitHub repo and push this code.
2. Add secrets to GitHub Secrets or your platform (see INSTALLATION.md).
3. Run locally with `docker-compose up --build`.
4. Use `scripts/clean-secrets.sh` if you suspect accidental leaks.
